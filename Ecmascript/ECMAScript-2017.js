/**Async functions (async/await)
 * Let us use synchronous-looking syntax to write asynchronous code.
 */

// => nothing to show

/**
 * Object.values() returns an Array with the values of all enumerable string-keyed properties of a
 * given object.
 */
Object.values();

/** String padding:
 * The string methods .padStart() and .padEnd() insert padding text until the receivers are long enough
 */
"7".padStart(3, "0"); // => '007'
"yes".padEnd(6, "!"); // => 'yes!!!'

/** Trailling commas in function parameter lists and calls
 * Trailling commas have been allowed in Arrays literals since ES3 and in Object literals since ES5.
 * They are now also allowed in function calls and method calls.
 */
